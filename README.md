# MineLab

Offline-first Flutter Android prototype for wet-lab experiment workflows.

Phase 0.1 includes a Dashboard timeline, CCK-8 template instantiation, step completion, planned-time editing, relative-time metadata, and local JSON persistence through `shared_preferences`. The data layer is intentionally isolated so it can migrate to SQLite/Drift as the schema grows.
